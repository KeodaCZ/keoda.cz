# dependencies & build output
node_modules/
dist/
.astro/

# owner's local brand/reference assets — never commit
Twitch and Youtube assets/

# local API credentials for running the fetch scripts by hand — never commit.
# The committed .env.example documents the names and holds no values.
.env
.env.*
!.env.example

# OS noise
Thumbs.db
.DS_Store

# local-only design playground — never deploy
src/pages/design-nahled.astro
public/fonts-preview/
